---
# Run all tests
stages:
  - lint
  - deployment test

before_script:
  - ansible --version
  - molecule --version
#  - ansible-config dump --only-changed

variables:
  GET_SOURCES_ATTEMPTS: "5"

Ansible lint:
  stage: lint
  before_script:
    - git clone https://github.com/lean-delivery/ansible-lint-rules.git ~/ansible-lint-rules
  script:
    - ansible-lint . -c .ansible-lint
  after_script:
    - rm -rf ~/ansible-lint-rules
  tags:
    - delegated

Docker java-6-jdk:
  stage: deployment test
  script:
    - molecule test -s docker-java-6-jdk
  tags:
    - aws

Docker java-7-jdk:
  stage: deployment test
  script:
    - molecule test -s docker-java-7-jdk
  tags:
    - aws

Docker java-8-jdk:
  stage: deployment test
  script:
    - molecule test -s docker-java-8-jdk
  tags:
    - aws

Docker java-8-jre:
  stage: deployment test
  script:
    - molecule test -s docker-java-8-jre
  tags:
    - aws

Docker java-8-server-jre:
  stage: deployment test
  script:
    - molecule test -s docker-java-8-server-jre
  tags:
    - aws

Docker java-11-jdk:
  stage: deployment test
  script:
    - molecule test -s docker-java-11-jdk
  tags:
    - aws

AWS java-8-jdk:
  variables:
    EPC_REGION: AWS-EUCENTRAL
  stage: deployment test
  script:
    - molecule test -s cloud-aws-delegated
  tags:
    - delegated

AWS Windows java-8-jdk:
  variables:
    AWS_REGION: us-east-1
  stage: deployment test
  script:
    - molecule test -s cloud-aws-windows
  tags:
    - aws

EPC java-8-jdk:
  variables:
    EPC_REGION: EPAM-BY2
  stage: deployment test
  script:
    - molecule test -s cloud-epc-delegated
  tags:
    - delegated
