---
requirements:
  # - python
  # - python-argparse
  - unzip
  - rsync

java_default_path: /usr/lib/jvm

java_folder_repo_transport: >-
  {{ 'java-' + java_major_version|string + '-openjdk-*'
      | default(omit) }}
